<!--pages/test/contact.wxml-->
<view class="contact">
  <van-index-bar>
    <view wx:for="{{list}}" wx:key="index">
      <van-index-anchor index="{{item.letter}}" />
      <view  wx:for="{{item.child}}" wx:key="index" >{{item.name}}</view>
    </view>
 
  </van-index-bar>
</view>