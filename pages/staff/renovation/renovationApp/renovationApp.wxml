<!--pages/staff/task/taskList/taskList.wxml-->
<!--pages/person/vista/vistaList/vistaList.wxml-->
<view class="vistaList">
  <van-tabs active="{{ active }}" color="#FFA477" bind:click="onClick">
    <van-tab title="处理中" title-style="{{current == 0?'font-weight:700;':''}}">
      <view class="card item task" wx:for="{{list}}" wx:key="index" bindtap="toDetail">
        <view class="title">
          <view class="titleL">
            {{item.title}}
          </view>
          <view class="titleR">{{item.time}}</view>
        </view>
        <view class="des">{{item.estimateTime}}</view>
        <view class="des">{{item.estimateTime}}</view>
        <view class="btn mt30">
        <view class="btnL">
          审核状态：<view class="numbered">{{item.status}}</view>
        </view> 
        </view>
      </view>
    </van-tab>
    <van-tab title="已结束" title-style="{{current == 1?'font-weight:700;':''}}">
      <view class="card item task" wx:for="{{list2}}" wx:key="index" bindtap="toDetail">
        <view class="title">
          <view class="titleL">
            {{item.title}}
          </view>
          <view class="titleR">{{item.estimateTime}}</view>
        </view>
        <view class="des">{{item.des}}</view>
        <view class="content">{{item.content}}</view>
      </view>
    </van-tab>
  </van-tabs>
</view>